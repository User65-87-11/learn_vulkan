ok createUniformBuffers33 
ok createDescriptorSetLayout2

ok createDescriptorSets33







--manager

typedef struct {
    VkImage image;
    VkImageView view;
    VkSampler sampler;

    uint32_t descriptorIndex; // index in descriptor array
    int alive;
} TextureEntry;

typedef struct {

	GmArray textures; // TextureEntry
   

    // descriptor stuff
    VkDescriptorSet descriptorSet;
    uint32_t binding; // e.g. BINDING_3D_SAMPLERS
} TextureManager;


typedef struct {
    VkDescriptorSetLayoutBinding bindings[MAX_BINDINGS];
    uint32_t bindingCount;

    VkDescriptorSetLayout layout;
    VkPipelineLayout pipelineLayout;
} ShaderInterface;

typedef struct {
    ShaderInterface* shader;

    VkDescriptorSet descriptorSet;
} Material;